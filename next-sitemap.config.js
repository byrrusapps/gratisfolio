/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://gratisfolio.web.app",
  generateRobotsTxt: true, // also creates /robots.txt
  changefreq: "weekly",
  priority: 0.7,
  sitemapSize: 5000,
};
